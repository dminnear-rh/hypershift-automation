---
- name: "IAM | Get the {{ iam.hcp_role_name }} role ARN"
  amazon.aws.iam_role_info:
    name: "{{ iam.hcp_role_name }}"
  register: result

- name: "IAM | Set the role ARN fact"
  ansible.builtin.set_fact:
    hcp_role_arn: "{{ result.iam_roles | map(attribute='arn') | first }}"
